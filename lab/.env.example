# Server
PORT=3001
NODE_ENV=production

# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=chronicle
DB_USER=chronicle
DB_PASS=changeme

# Auth
JWT_SECRET=changeme-generate-a-real-secret
JWT_EXPIRES_IN=604800

# CORS — frontend origin
CORS_ORIGIN=https://app.chronicle.cz

# Socket.IO
SOCKET_PATH=/socket.io
